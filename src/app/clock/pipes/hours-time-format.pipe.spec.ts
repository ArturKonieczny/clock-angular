import { HoursTimeFormatPipe } from './hours-time-format.pipe';

describe('HoursTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HoursTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
