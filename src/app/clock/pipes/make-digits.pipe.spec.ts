import { MakeDigitsPipe } from './make-digits.pipe';

describe('MakeDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new MakeDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
