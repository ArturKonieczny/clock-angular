import { AnalogAnglePipe } from './analog-angle.pipe';

describe('AnalogAnglePipe', () => {
  it('create an instance', () => {
    const pipe = new AnalogAnglePipe();
    expect(pipe).toBeTruthy();
  });
});
